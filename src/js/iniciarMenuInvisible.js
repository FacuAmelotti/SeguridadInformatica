// Llamar a la función para ocultar el menú cuando la página cargue
document.addEventListener('DOMContentLoaded', () => {
    const menu = document.getElementById('menu');
    const content = document.querySelector('.content');

        desplegarAction();

});
